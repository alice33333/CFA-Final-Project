<% provide(:title, 'Front of House Averages') %>
<h2>Front of House Averages</h2>

     <% @users.each do |a, i| %>
     <% if a.role == 1 || a.role == 4 %>
       <div class="form-inline">
    <div class="form-group">
        <h2><%= i%> </h2>
        <h2><%= a.name.to_s %> </h2>
        </div>
        <div class="form-group">
        <h2><%= a.overall.to_s %> </h2>
        </div>
    </div>
    <% end %>
    <% end %>