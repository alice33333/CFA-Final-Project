<% provide(:title, 'Back of House Averages') %>
<h2>Back of House Averages</h2>
    
    <% @users.each do |a, i| %>
     <% if a.role == 5 || a.role == 2 %>
       <div class="form-inline">
    <div class="form-group">
        <h2><%= i%> </h2>
        <h2><%= a.name.to_s %> </h2>
        </div>
        <div class="form-group">
        <h2><%= a.overall.to_s %> </h2>
        </div>
    </div>
    <% end %>
    <% end %>